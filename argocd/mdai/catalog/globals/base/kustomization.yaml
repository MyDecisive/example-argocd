apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Minimal baseline: the hub + one collector definition.
# Team/environment bundles layer solutions and overrides on top.

resources:
  - hub.yaml
  - otel.yaml
